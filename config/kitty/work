new_tab Server
layout grid
cd ~/dev/jeancaisse
launch --var window=first --title="rails server" bin/rails server

new_tab Sidekiq
layout grid
cd ~/dev/jeancaisse
launch --location=vsplit --title="rails server" bin/sidekiq

new_tab Frontend
layout grid
cd ~/dev/jeancaisse
launch --title="rails server" yarn start

# vim: ft=kitty
