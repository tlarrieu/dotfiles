"dans quel sens les autocompletions sont-elles proposées
set complete=slf

" comportement de la completion
set wildmode=list:longest,list:full

"visualbell
set novisualbell

" comportement de la casse lors de la recherche
set ignorecase
set smartcase

" une fois qu’une page est chargée, ne pas aller automatiquement dans les champs de saisie
set focuscontent

" History navigation
noremap b :back<cr>
noremap B :forward<cr>

noremap à d
noremap O t

noremap é /
noremap É ?
noremap \' n
noremap \? N

" {cr} = « gauche / droite »
noremap c <Left>
noremap r <Right>
" {ts} = « haut / bas »
noremap t <Down>
noremap s <Up>


" Désambiguation de {g}
" —————————————————————
" ligne précédante/suivante (à l'intérieur d'une phrase)
noremap gs gk
noremap gt gj

noremap <BS> <C-u>
noremap <Return> zz

" va a l’onglet suivant/précédent
map <Left> gb
map <Right> gé
noremap T gt
noremap S gT
