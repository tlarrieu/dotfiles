#!/bin/sh

echo "$1" | grep jobteaser > /dev/null

if [[ $? -eq 0 ]]; then
  profile="pro"
else
  profile="perso"
fi

if [[ "$1" == "https"* ]]; then
  chromium-kiosk $profile "$1"
else
  chromium-kiosk $profile "https://www.notion.so/$1"
fi

